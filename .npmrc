# Keep installation independent of the registry's advisory service.
# Run npm audit explicitly when reviewing dependencies.
audit=false
fund=false
prefer-offline=true
fetch-retries=1
fetch-timeout=60000
